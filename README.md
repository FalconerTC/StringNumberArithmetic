# StringNumberArithmetic
Defines the struct `StringNumber` with the functions `Add` and `Multiply`. Any non-numeric values are treated as 0s

## Run example app
make build && make run

## Run tests
make test